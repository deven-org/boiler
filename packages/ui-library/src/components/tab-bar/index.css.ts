import { css } from "lit";

export const styleCustom = css`
  :host {
    display: inline-flex;
    flex-shrink: 0;
    width: 100%;
  }

  .wrapper-horizontal {
    position: relative;
    display: block;
  }
`;
