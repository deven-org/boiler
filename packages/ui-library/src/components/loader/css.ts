import { css } from 'lit';

export const styleCustom = css`
  :host {
    display: inline-flex;
    flex-shrink: 0;
  }
  .blr-loader {
    position: relative;
    display: inline-flex;
    flex-shrink: 0;
  }
`;
