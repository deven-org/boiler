import { css } from 'lit';

export const styleCustom = css`
  .blr-input {
    display: block;
    width: 100%;
    position: relative;
  }

  .blr-text-input:disabled {
    pointer-events: none;
  }
`;
