import { css } from 'lit';

export const styleCustom = css`
  .blr-text-button {
    cursor: pointer;
    padding: 0;
  }

  .blr-text-button .icon {
    flex-shrink: 0;
    width: 12px;
    height: 12px;
  }
`;
